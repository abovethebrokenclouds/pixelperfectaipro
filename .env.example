# Frontend (build-time, safe to expose)
VITE_API_BASE_URL=http://localhost:4000

# Backend (server-side only — never commit real values)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5
PORT=4000
CORS_ORIGIN=http://localhost:3000
RENDER_WORKER_URL=
STORAGE_BUCKET=
